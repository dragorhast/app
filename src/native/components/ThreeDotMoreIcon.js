import React from 'react';
import { Icon } from 'expo';

// isAndroid ? 'md-more' : 'ios-more'
const ThreeDotMoreIcon = () => <Icon.Ionicons name="md-more" size={32} />;

export default ThreeDotMoreIcon;
